# Product Requirements Document (PRD)

## Product

AI Release Notes GitHub Action (`castoff`)

## Status

Draft v1 (based on current README + code behavior)

## 1. Problem Statement

Engineering teams spend time manually writing release notes from commit history. Existing autogenerated notes are often noisy and not grouped by user-facing value. Teams need consistent, readable release notes with minimal manual effort in CI/CD workflows.

## 2. Product Overview

`castoff` is a GitHub Action that generates Markdown release notes using OpenAI models. It gathers recent commit messages from git history, prompts a model to summarize them into structured sections, and exposes the result as an action output consumable by release workflows.

## 3. Target Users

- Maintainers of GitHub repositories shipping frequent releases
- Teams using release automation workflows (Node/Python examples in this repo)
- Developers who want higher-quality notes than raw commit logs

## 4. Goals

- Generate release notes automatically in CI with minimal setup
- Produce clean Markdown suitable for GitHub Releases
- Keep behavior deterministic and easy to integrate via standard action inputs/outputs
- Support a safe fallback path when AI output is unavailable

## 5. Non-Goals

- Auto-publishing releases directly (handled by downstream release actions/workflows)
- Full semantic analysis of code diffs (input is commit subject lines)
- Multi-language localization of release notes
- Committing or pushing changelog files from within the action (the action
  returns entry text; workflows write and commit it)

## 6. User Stories

1. As a maintainer, I can provide my release tag and OpenAI API key to get release notes text as workflow output.
2. As a release engineer, I can choose an OpenAI model without editing action code.
3. As a workflow author, I can limit commit volume to control token/cost usage.
4. As a maintainer, I receive a fallback note body if AI response content is missing.
5. As a maintainer, I can maintain a `CHANGELOG.md` from the same generated notes without writing a second generator.

## 7. Functional Requirements

### 7.1 Inputs

- `openai_api_key` (required): API key used to call OpenAI.
- `tag` (required): Release tag string for prompt context and fallback title.
- `model` (optional): OpenAI model name. The explicit input takes precedence over the `OPENAI_MODEL` environment variable; if neither is nonempty, use `gpt-6-astra`.
- `max_commits` (optional, default `200`): Max number of commits read from git log.

### 7.2 Output

- `model`: Resolved model name sent to OpenAI.
- `release_notes`: Markdown string suitable for use as GitHub Release body.
- `changelog_entry`: Markdown string suitable for insertion into a Keep a Changelog file.

#### 7.2.1 Generator attribution (issue #21)

- After generation, including fallback notes, append `---` followed by: Generated by [Castoff](https://github.com/everydaydevopsio/castoff) using OpenAI model `<resolved model>`.
- Use the resolved model sent in the request and exposed as the `model` output: explicit input, then trimmed nonempty `OPENAI_MODEL`, then `gpt-6-astra`.
- Emit exactly one trailing attribution footer; replace existing trailing Castoff attribution footers, including stale model values.
- Preserve the preceding note content and section structure, apart from existing outer whitespace trimming.

#### 7.2.2 Changelog entry

- Emit a `changelog_entry` output derived from the same generated notes, including fallback notes.
- Head the entry with `## [<version>] - <YYYY-MM-DD>`, using the `tag` input without a leading `v` and the release date in UTC.
- Demote ATX headings one level so note sections nest under the version heading; leave level-six headings and headings inside fenced code blocks unchanged.
- Omit the attribution footer so a file accumulating entries does not repeat it.
- Emit `_No release notes were generated._` as the body when the notes carry no content.

### 7.3 Commit Range and Collection

- Action attempts to detect previous tag via `git describe --tags --abbrev=0 HEAD^`.
- If previous tag exists, commit range is `<previousTag>..HEAD`.
- If no previous tag is found, range is `HEAD` and action logs informational message.
- Commit messages are read using `git log --pretty=format:'%h %s' ... -n <max_commits>`.
- Empty or whitespace lines are filtered; non-empty commits are formatted as bullet list entries.
- `chore: release <version>` commits are filtered: the release workflow commits one before generating notes, and it describes the release rather than a change within it.

### 7.4 Prompting Behavior

- Prompt includes current release tag.
- Prompt includes previous tag (or `None`).
- Prompt includes commit bullet list (or `_No commits found_`).
- Prompt includes formatting requirements for `## Highlights`, `## Fixes`, and `## Changes`.
- Action sends a system message positioning model as an expert technical writer.
- Action sends a user message containing the generated prompt.
- Omit the OpenAI request `temperature` parameter for compatibility with `gpt-6-astra`.

### 7.5 Success and Failure Behavior

- On successful completion, action sets `release_notes` output and logs success message.
- If OpenAI response content is empty/missing, action returns fallback heading `# Release <tag>`.
- If OpenAI response content is empty/missing, action returns fallback text `_Auto-generated notes unavailable._`.
- On runtime error (input issues, git failure, API failure, etc.), action fails using `core.setFailed(error.message)` for an `Error`, or the string representation of a non-Error exception.

## 8. Non-Functional Requirements

- Runtime compatibility: GitHub Actions Node.js 24, as declared in `castoff/action.yml`.
- Reliability: Must fail fast with clear error on unrecoverable runtime exceptions.
- Security: OpenAI key provided via Actions secrets; action must not print key.
- Performance: Should complete within typical workflow step timeouts for <=200 commits.
- Maintainability: Core logic remains testable via exported pure helpers (`formatCommits`, `buildPrompt`, `extractNotes`).

## 9. UX and Integration Requirements

- Setup should require only one action step plus release step wiring.
- Output must be directly consumable by `softprops/action-gh-release` `body` input.
- Documentation should include minimal usage and fallback strategy.

## 10. Metrics and Success Criteria

- Adoption: Number of workflows invoking the action (downstream signal).
- Quality proxy: Maintainer-reported reduction in manual release-note editing.
- Reliability metric: Action success rate in CI runs.
- Reliability metric: Percentage of runs producing non-fallback AI content.
- Cost control: Commit cap usage via `max_commits`.

## 11. Constraints and Dependencies

- Requires git history/tags availability in workflow checkout.
- Requires valid OpenAI API key and network access to OpenAI API.
- Output quality depends on commit message quality and structure.
- Token/cost footprint scales with commit volume and message length.

## 12. Risks

- Poor commit hygiene can degrade summary quality.
- Missing tag history or shallow clones may reduce context.
- Model output variability may produce inconsistent section balance.
- API outages or quota limits can fail the action.

## 13. Assumptions

- Repositories follow tag-based release workflows.
- Commit subjects provide enough signal for meaningful summaries.
- Consumers prefer concise human-readable summaries over exhaustive logs.

## 14. Acceptance Criteria (v1)

1. Given required inputs, action emits non-empty `release_notes` output.
2. For repos with no previous tag, action still runs and includes `Previous tag: None` context.
3. For empty OpenAI content, fallback markdown is returned with the provided tag.
4. In release workflow usage, output can be passed directly as GitHub Release body.
5. Unit tests for helper functions pass for formatting, prompt construction, and extraction fallback behavior.

## 15. Deployment Pipeline

### 15.1 Artifact Model

- The repository is a pnpm workspace: the root holds git hooks, formatting and shared tooling, and `castoff/` holds the action. One lockfile lives at the root.
- The pnpm version is pinned by the root `packageManager` field, so Corepack, CI and the release workflow all resolve the same version.
- Husky installs git hooks through the root `prepare` script. `pre-commit` runs lint-staged across the repository; `pre-push` builds the bundle, verifies it matches the committed one, and runs the tests.
- Action source and tests are authored in TypeScript with strict type checking.
- `pnpm --filter castoff typecheck` validates source and tests; `pnpm --filter castoff build` type-checks before bundling `main.ts` with `@vercel/ncc` into `dist/index.js`.
- The TypeScript migration preserves existing action inputs, outputs, prompts, and commit collection behavior.
- `action.yml` references `dist/index.js` (not `index.js`) so consumers do not need `node_modules/`.
- `dist/` is committed into git as part of each release commit.

### 15.2 Release Workflow (`.github/workflows/release.yml`)

- Triggered manually via `workflow_dispatch` with `level` input (`patch` | `minor` | `major`).
- Steps in order:
  1. Require a nonempty `OPENAI_API_KEY` Actions secret, then run the full test suite. Fail before release changes if either check fails.
  2. Bump `package.json` version via `npm version <level> --no-git-tag-version`.
  3. Compile `dist/index.js` via `pnpm --filter castoff build` (ncc).
  4. Commit `package.json` and `dist/` locally with message `chore: release vX.Y.Z`.
  5. Generate AI release notes using the action and `OPENAI_MODEL` repository variable if configured. The local release commit keeps the previous-tag lookup correct; API failures stop the workflow before publishing.
  6. Insert the `changelog_entry` output into `CHANGELOG.md` via `scripts/update-changelog.sh`, then amend the release commit so the tag carries its own changelog. Pass the entry through the environment; generated notes are data.
  7. Create an exact version tag (`vX.Y.Z`) and update the floating major tag (`vN`) in place.
  8. Push all tags and the commit to `main`.
  9. Create a GitHub Release for `vX.Y.Z` with the action output; do not fall back to GitHub-generated notes.

### 15.3 Changelog File (`CHANGELOG.md`)

- `scripts/update-changelog.sh` takes a semantic version and an entry on standard input.
- It creates `CHANGELOG.md` with a Keep a Changelog header when the file is absent.
- It inserts the entry above the newest existing entry, below an `## [Unreleased]` heading when one is present, or at the end of the file when it holds no releases.
- It exits successfully without changes when the version already has a heading, so reruns of a failed release are safe.
- It rejects an empty entry, and any version outside the SemVer 2.0.0 grammar: a leading `v`, leading zeros in the numeric parts, and empty or leading-dot prerelease or build identifiers.
- Entry text is passed to `awk` through the environment so it is never parsed as syntax.
- It stages the rewritten file beside the target and renames it into place, so an interrupted run leaves the existing changelog intact. The staging copy inherits the target's mode, which the rename would otherwise replace.

### 15.4 CI Workflow (`.github/workflows/ci.yml`)

- Triggered on push and pull requests to `main`.
- Jobs: `test` (strict TypeScript check, bundle build and freshness check, Jest with coverage ≥ 75%) and `lint` (TypeScript-aware ESLint, plus a Prettier check covering the whole repository).
- Uses concurrency cancel-in-progress to avoid redundant runs.

### 15.5 Consumer Usage

Users reference the action by major version tag, which the release workflow
moves to each release in that major series:

```yaml
uses: everydaydevopsio/castoff/castoff@v2
```

Or by exact version for pinning:

```yaml
uses: everydaydevopsio/castoff/castoff@v2.0.0
```

The reusable workflows under `examples/` are referenced by the same major tag and
expose `use_ai_release_notes` and `update_changelog` inputs. They push the release
tag before generating notes, so they record the changelog as a follow-up commit
rather than amending the tagged commit.

### 15.6 Live E2E Workflow

- Automatically run after `CI` (`ci.yml`) completes successfully for a push to `main`; skip automatic jobs for failed/cancelled CI or pull-request CI.
- Check out the triggering CI run's exact head commit, even when `main` advances before E2E starts.
- Keep manual `workflow_dispatch` runs and their optional tag input available.
- Isolate E2E concurrency by run ID so a later merge or manual run cannot cancel or replace another run. Only commits whose CI completes successfully qualify; CI runs cancelled by newer pushes do not qualify.
- Test the default `gpt-6-astra` without model configuration and `gpt-5.6-sol` via `OPENAI_MODEL`, both using the `OPENAI_API_KEY` Actions secret.
- Test an additional configured model when the repository/ACT `OPENAI_MODEL` variable is nonempty.
- Verify missing-key failures and reject placeholder fallback notes as live-test success.
- Verify the `changelog_entry` output is headed by the release version and an ISO date, and carries no canonical attribution footer line. Match that line exactly: entry content may legitimately discuss the footer.
- The local ACT launcher forwards environment configuration; missing keys fail inside the workflow.

## 16. Future Enhancements (Post-v1)

- Configurable prompt templates and section headings.
- Optional inclusion of PR titles, labels, or conventional-commit grouping.
- Built-in retry/backoff on transient API errors.
- Optional style profiles (short, detailed, user-facing, technical).
- Repository-specific instructions input for tone and structure.
